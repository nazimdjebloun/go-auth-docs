---
title: "Routes"
description: "Every HTTP route go-auth mounts, grouped by area, with path params, query params, and request body fields."
icon: "Route"
---

# Routes

This page covers every route `Auth.Mount(mux)` registers. Paths in the tables use `{name}` for a path parameter read via `r.PathValue("name")`. "Auth" is the minimum session/role required — see [Architecture](architecture.mdx#the-middleware-chain) for the full middleware chain each route runs through, and [Error Handling](error-handling.mdx) for what each one returns on failure.

A few routes are conditionally mounted and simply don't exist on the mux otherwise:

- Everything under **Invites** requires `RegistrationConfig.EnableInvite: true`.
- Everything under **OAuth** requires `RegistrationConfig.EnableOAuth: true` and at least one `WithProvider` call.
- Everything under **Organizations** requires `WithOrganizations(OrganizationConfig{Enable: true})`.
- `POST /auth/register` requires `RegistrationConfig.EnableEmailPassword: true`.

Whenever `SecurityConfig.AllowedOrigins` is non-empty, Mount also registers an `OPTIONS` twin for every distinct path (deduplicated across methods sharing one), short-circuited to `204` by the CORS middleware before reaching any handler — these aren't listed separately below.

## Authentication

| Method & Path | Auth | Body | Description |
|---|---|---|---|
| `POST /auth/register` | Public | `` `{email, password, name}` `` | Register with email/password. Returns a session unless verification is required, or `RequireEmail2FA` is on (see [Two-factor authentication](#two-factor-authentication)). |
| `POST /auth/login` | Public | `` `{email, password}` `` | Log in. May return `requiresVerification: true` instead of a session, or `requiresTwoFactor: true` if the account has 2FA on. |
| `POST /auth/admin/login` | Public | `` `{email, password}` `` | Same as login, but only succeeds for `role: admin` users. Requires a second factor unless `SecurityConfig.DisableAdminTwoFactor` is set — see [Two-factor authentication](#two-factor-authentication). |
| `POST /auth/logout` | Auth | none | Revokes the current session and clears its session + refresh cookies. |
| `GET /auth/me` | Auth | none | Returns the authenticated user plus `hasPassword`. |
| `GET /auth/check` | Public (session cookie optional) | none | Soft check — always `200`, returns `{"user": null}` instead of erroring when there's no valid session. |
| `GET /auth/csrf-token` | Public | none | No-op handler (`204`) — the CSRF cookie/header is actually issued by the CSRF middleware wrapping this route. Call it to prime the cookie before your first mutating request. |
| `PUT /auth/name` | Auth | `` `{name}` `` | Updates the authenticated user's display name. |

## Sessions

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /auth/sessions` | Auth | Query: `offset` (int, default `0`), `limit` (int, default `20`, max `100`) | Paginated list of your own sessions, plus `currentSessionId`. |
| `GET /auth/sessions/all` | Auth | none | All of your sessions, unpaginated, plus `currentSessionId`. |
| `DELETE /auth/sessions/{id}` | Auth | Path: `id` | Revokes one of your own sessions. `404 session_not_found` if it doesn't exist or isn't yours — the two cases are indistinguishable on purpose. |
| `POST /auth/sessions/revoke` | Auth | Body: `` `{sessionIds: []string}` `` | Bulk-revokes up to 100 of your own session IDs at once; unrecognized/foreign IDs are silently skipped. Returns `revoked` count. |
| `DELETE /auth/sessions` | Auth | none | Revokes all your sessions **except** the one making the request (or all of them if the current one can't be resolved). |
| `POST /auth/refresh` | Public (refresh cookie required) | Cookie: refresh token cookie | Rotates the session using the refresh cookie. Sets new session+refresh cookies on success; clears both on failure. |

## Password

| Method & Path | Auth | Body | Description |
|---|---|---|---|
| `POST /auth/forgot-password` | Public | `` `{email}` `` | Always returns a generic success message, whether or not the account exists. |
| `POST /auth/reset-password` | Public | `` `{code, newPassword}` `` | Completes a password reset using the emailed code. |
| `POST /auth/change-password` | Auth | `` `{oldPassword, newPassword}` `` | Changes the authenticated user's password; revokes every other session. |
| `POST /auth/set-password/request` | Auth | none | Sends a set-password email link — for OAuth-only accounts with no password yet. |
| `POST /auth/set-password/confirm` | Public | `` `{userId, code, newPassword}` `` | Confirms a set-password request. Unauthenticated by design — `userId` comes from the body, not a session. |

## Account

| Method & Path | Auth | Body | Description |
|---|---|---|---|
| `DELETE /auth/account` | Auth | `` `{password}` `` | Deletes the account immediately after password confirmation. |
| `POST /auth/account/delete/request` | Auth | none | Emails a deletion confirmation code. For OAuth-only accounts (no password) instead of the immediate-delete route above. |
| `POST /auth/account/delete/confirm` | Auth | `` `{code}` `` | Confirms deletion with the emailed code. The user is always the authenticated session, never taken from the body. |

## Email verification

| Method & Path | Auth | Body | Description |
|---|---|---|---|
| `POST /auth/verify-email` | Public | `` `{code}` `` | Verifies the email and immediately creates a session for the now-verified user. |
| `POST /auth/resend-verification` | Auth | none | Resends the verification email to the authenticated (unverified) user. |
| `POST /auth/verify-email/resend` | Public | `` `{email}` `` | Unauthenticated resend by email address. Always returns a generic success message — no enumeration. |

## Two-factor authentication

`Login`, `AdminLogin`, `Register`, and `POST /auth/invite/register` all return the same gated shape when a second factor is still owed: `{user, requiresTwoFactor: true, codeSent, challengeId, expiresAt, message}` — no session/refresh cookie, no CSRF rotation. Alongside that body, the response sets a binding cookie (name from `SecurityConfig.TwoFactorChallengeCookieName`, default `_2fa_challenge`; HttpOnly; same `Domain`/`Path`/`Secure`/`SameSite` as the session cookie; `MaxAge` = `TwoFactorCodeTTL`) that ties the challenge to the browser that started it. `POST /auth/2fa/verify` and `POST /auth/2fa/resend` both require that cookie back — a `challengeId` alone (e.g. one that leaked into a log line) is not a usable credential without it. The cookie is skipped entirely when `SecurityConfig.DisableTwoFactorChallengeBinding` is set, for non-browser consumers.

| Method & Path | Auth | Body | Description |
|---|---|---|---|
| `POST /auth/2fa/verify` | Public (requires the binding cookie, unless disabled) | `` `{challengeId, code}` `` | Completes any of the four gated flows above. Sets session+refresh cookies and clears the binding cookie on success. Wrong code, wrong binding cookie, and a capped lineage (5 wrong guesses) all return the same `two_factor_code_invalid` — see [Error Handling](error-handling.mdx). |
| `POST /auth/2fa/resend` | Public (requires the binding cookie, unless disabled) | `` `{challengeId}` `` | Refreshes the code on the same challenge — the `challengeId` doesn't change. Capped at 3 resends per challenge; an unknown or binding-mismatched challenge gets the same generic `200` as a real send, to avoid enumeration. |
| `POST /auth/2fa/enable` | Auth | `` `{password, keepOtherSessions}` `` | Turns on per-user 2FA after re-checking the password. `keepOtherSessions` defaults to `false` (omitted = revoke every other session) — spelled this way so the zero value is the safe one, matching `DisableCSRFToken`. Rejected with `two_factor_already_enforced` if `RequireEmail2FA` is on. |
| `POST /auth/2fa/disable` | Auth | `` `{password}` `` | Turns off per-user 2FA after re-checking the password. No session revocation — turning 2FA off isn't the "someone may be in my account" signal that turning it on is. Rejected with `two_factor_already_enforced` if `RequireEmail2FA` is on. |

## Invites (self-service signup) — requires `EnableInvite`

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /auth/invite/info` | Public | Query: `token` (required) | Looks up an invite by token — used to pre-fill a registration form. `400 missing_token` if omitted. |
| `POST /auth/invite/register` | Public | Body: `` `{code, name, password, confirmPassword}` `` | Completes registration from an invite code, creating the account and a session in one step — unless `RequireEmail2FA` is on, in which case it returns a gated response instead; see [Two-factor authentication](#two-factor-authentication). |

## OAuth — requires `EnableOAuth` and at least one registered provider

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /auth/oauth/{provider}` | Public | Path: `provider` | Starts the OAuth flow. Returns `` `{url}` `` to redirect the browser to. |
| `GET /auth/oauth/{provider}/callback`, `POST /auth/oauth/{provider}/callback` | Public | Path: `provider`; form values `code`, `state` (from query on `GET`, parsed form body on `POST`) | Completes the code exchange. On success, sets cookies and redirects to `{BaseURL}/auth/callback`; on error, redirects to `{BaseURL}/auth/callback?error={code}&provider={provider}` — see [Error Handling](error-handling.mdx#oauth). |
| `POST /auth/oauth/{provider}/link` | Auth | Path: `provider` | Links a provider to the already-authenticated account. Returns `` `{url}` ``. |
| `POST /auth/oauth/{provider}/unlink` | Auth | Path: `provider` | Unlinks a provider. Blocked if it would leave the account with no way to log in. |
| `GET /auth/oauth/providers` | Auth | none | Lists connected providers — `provider`, `email`, `name`, `avatarUrl`, `createdAt` only. Access/refresh tokens are never returned. |

## Organizations — requires `WithOrganizations(Enable: true)`

On every listing below that takes a `role` filter (and on the admin equivalents further down), an unrecognized value is a **400**, not a silently dropped filter — unlike `orderBy`/`orderDirection`, which fall back to their defaults. An empty `role=` means "no filter". See the callout in [Organizations](guides/organizations.mdx#list-your-orgs--get-authorgs) for why the two differ.

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `POST /auth/orgs` | Auth | Body: `` `{name, slug}` `` | Creates an org owned by the caller. |
| `GET /auth/orgs` | Auth | Query: `role` (`owner`\|`admin`\|`member`), `search`, `orderBy` (`name` default, `created_at`, `member_count`), `orderDirection` (`asc` default, `desc`), `offset`, `limit` (omit for default `20`; `0` = unlimited; else capped at `100`) | Lists the orgs the caller belongs to. No total in the response — see `/count`. |
| `GET /auth/orgs/count` | Auth | Query: `role`, `search` | Count of orgs the caller belongs to matching the filters. |
| `GET /auth/orgs/{orgID}` | Org member | Path: `orgID` | Fetches one org. |
| `PUT /auth/orgs/{orgID}` | Org admin | Path: `orgID`; body: `` `{name?, slug?}` `` (both pointers — omit a field to leave it unchanged) | Partial update of org name/slug. |
| `DELETE /auth/orgs/{orgID}` | Org owner | Path: `orgID` | Deletes the org. |
| `GET /auth/orgs/{orgID}/members` | Org member | Path: `orgID`; query: `role`, `search`, `orderBy` (`joined_at` default, `role`, `name`, `email`), `orderDirection` (`asc` default, `desc`), `offset`, `limit` (omit for default `20`; `0` = unlimited; else capped at `100`) | Lists org members. No total in the response — see `/count`. |
| `GET /auth/orgs/{orgID}/members/count` | Org member | Path: `orgID`; query: `role`, `search` | Count of the org's members matching the filters. |
| `DELETE /auth/orgs/{orgID}/members/{userID}` | Org admin | Path: `orgID`, `userID` | Removes a member. |
| `PATCH /auth/orgs/{orgID}/members/{userID}/role` | Org admin | Path: `orgID`, `userID`; body: `` `{role}` `` | Changes a member's role. |
| `POST /auth/orgs/{orgID}/leave` | Org member | Path: `orgID` | The caller leaves the org. |
| `PUT /auth/orgs/active` | Auth | Body: `` `{orgId}` `` | Sets the caller's active org for the current session. No `{orgID}` path segment — membership is checked inside the service, not by middleware. |
| `DELETE /auth/orgs/active` | Auth | none | Clears the active-org setting for the current session. |
| `POST /auth/orgs/{orgID}/invites` | Org admin | Path: `orgID`; body: `` `{email, role}` `` | Creates an invite to join the org. |
| `POST /auth/orgs/invites/accept` | Auth | Body: `` `{code}` `` | Accepts an org invite by code. No path segment — the invite is resolved from the code. |
| `GET /auth/orgs/{orgID}/invites` | Org admin | Path: `orgID`; query: `role`, `status` (`pending`/`expired`, default both), `search`, `orderBy` (`created_at` default, `expires_at`, `email`, `role`), `orderDirection` (`desc` default, `asc`), `offset`, `limit` (omit for default `20`; `0` = unlimited; else capped at `100`) | Lists an org's invites. No total in the response — see `/count`. |
| `GET /auth/orgs/{orgID}/invites/count` | Org admin | Path: `orgID`; query: `role`, `status`, `search` | Count of the org's invites matching the filters. |
| `POST /auth/orgs/{orgID}/invites/{inviteID}/resend` | Org admin | Path: `orgID`, `inviteID` (`orgID` is only used for the authorization check, not read again inside the handler) | Resends an org invite email. |
| `DELETE /auth/orgs/{orgID}/invites/{inviteID}` | Org admin | Path: `orgID`, `inviteID` (same note as above) | Deletes an org invite. |

## Admin — users

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /admin/users` | Admin | Query: `offset`, `limit` (default `20`, max `100`); `email`, `search` (optional filters); `role` (`admin` or `user` only — any other value is ignored, not applied); `isBanned` (`true`\|`false`); `isVerified` (`true`\|`false`); `twoFactorEnabled` (`true`\|`false`); `neverLoggedIn` (`true` only — `last_login_at IS NULL`); `lastLoginBefore` (RFC3339 — `last_login_at < X`, excludes never-logged-in); `orderBy` (`created_at` \| `updated_at`, default `created_at`); `orderDirection` (`asc` \| `desc`, default `desc`) | Paginated, filterable, sortable user listing. No total in the response — see `/count`. |
| `GET /admin/users/count` | Admin | Same filter params as `GET /admin/users` (`offset`/`limit`/`orderBy`/`orderDirection` ignored) | Count of users matching the filters — fetched on its own cadence so paging doesn't re-run a `COUNT(*)`. |
| `GET /admin/users/{id}` | Admin | Path: `id` | Full detail for one user. |
| `POST /admin/users` | Admin | Body: `` `{email, password, name, role}` `` | Admin-creates a user with a specified role. |
| `PATCH /admin/users/{id}/role` | Admin | Path: `id`; body: `` `{role}` `` | Sets a user's role. Blocked if it would demote the last admin. |
| `PATCH /admin/users/{id}/ban` | Admin | Path: `id` | Bans a user. Blocked for the last admin. |
| `PATCH /admin/users/{id}/unban` | Admin | Path: `id` | Unbans a user. |
| `DELETE /admin/users/{id}` | Admin | Path: `id` | Deletes a user. Blocked for the last admin. |
| `GET /admin/users/{id}/sessions` | Admin | Path: `id`; query: `offset`, `limit` (default `20`, max `100`) | Paginated sessions for a specific user. |
| `DELETE /admin/users/{id}/sessions/{sessionId}` | Admin | Path: `id`, `sessionId` | Revokes one specific session belonging to a specific user. |
| `DELETE /admin/users/{id}/sessions` | Admin | Path: `id` | Revokes all of a user's sessions. |
| `GET /admin/sessions` | Admin | Query (all optional): `offset`, `limit` (default `20`, max `100`); `userId`, `ip`, `search` (substring-matches `ip`/user agent); `createdAfter`, `createdBefore`, `expiresAfter`, `expiresBefore`, `lastActiveAfter`, `lastActiveBefore` (RFC3339); `orderBy` (`created_at` default, `expires_at`, `last_active_at`); `orderDirection` (`desc` default, `asc`) | Active sessions across every user — the incident-response view ("who's logged in right now", "every session from this IP"). No total in the response — see `/count`. |
| `GET /admin/sessions/count` | Admin | Same filter params as `GET /admin/sessions` (pagination/ordering ignored) | Count of active sessions matching the filters. |
| `POST /admin/users/bulk/ban` | Admin | Body: `` `{userIds: string[]}` `` (max 100) | Bans each user independently — not atomic. Response: `` `{succeeded: string[], failed: [{userId, code, message}]}` ``. |
| `POST /admin/users/bulk/unban` | Admin | Body: `` `{userIds: string[]}` `` (max 100) | Same shape as bulk ban, for unbanning. |
| `POST /admin/users/bulk/delete` | Admin | Body: `` `{userIds: string[]}` `` (max 100) | Same shape as bulk ban, for deletion. Blocked per-user for the last admin. |
| `POST /admin/users/bulk/revoke-sessions` | Admin | Body: `` `{userIds: string[]}` `` (max 100) | Same shape as bulk ban — revokes every session for each listed user. |

## Admin — audit logs

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /admin/audit-logs` | Admin | Query (all optional): `offset`, `limit` (default `50`, max `200`); `event_type` (comma-separated, matches any); `actor_id`/`actorEmail`, `target_user_id`/`targetEmail` (email resolved to ID server-side, `404 user_not_found` if none), `session_id`, `org_id`, `deviceType` (`mobile`/`desktop`/`tablet`/`bot`), `ip`, `search` (substring across metadata/user agent/IP/event type); `from`, `to` (RFC3339, independently optional, silently ignored if unparsable); `success` (`true` \| `false`) | Filtered, paginated audit events. Empty (not an error) if audit logging isn't enabled. No total in the response — see `/count`. |
| `GET /admin/audit-logs/count` | Admin | Same filter params as `GET /admin/audit-logs` (pagination ignored) | Count of audit events matching the filters. |
| `GET /admin/users/{id}/audit-logs` | Admin | Path: `id`; same query params as above | Same as above, pre-filtered to that user. |
| `GET /admin/users/{id}/audit-logs/count` | Admin | Path: `id`; same filter params | Count of that user's audit events matching the filters. |

## Admin — stats and activity

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /admin/stats` | Admin | none | Overview counts: total/verified/banned/two-factor/never-logged-in users, active sessions. |
| `GET /admin/stats/registrations` | Admin | Query: `from`, `to` (RFC3339, both required, 400-day max span) | Registrations per day — a registrations-over-time chart. |
| `GET /admin/stats/logins` | Admin | Query: `from`, `to` (RFC3339, both required, 400-day max span); `userId` (optional — global if omitted) | Successful (`login.success`) logins per day — a GitHub-commit-style login heatmap. Empty if audit logging is disabled. |

## Admin — organizations (`WithOrganizations` required)

Platform-admin oversight of orgs — act on any organization regardless of the caller's own membership in it. Distinct from the `/auth/orgs/*` routes above: those enforce org-membership roles (`Org member`/`Org admin`/`Org owner`), these enforce platform `Admin` only. Every mutation here publishes its own `admin.org.*` audit event, distinct from the self-service `organization.*` events — see [Audit Logs](guides/audit-logs.mdx).

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `GET /admin/orgs` | Admin | Query (all optional): `search` (matches name/slug), `createdAfter`, `createdBefore` (RFC3339), `orderBy` (`name` default, `created_at`, `member_count`), `orderDirection` (`asc` default, `desc`), `offset`, `limit` (omit for default `20`; `0` = unlimited; else capped at `100`) | Every organization on the platform. No total in the response — see `/count`. |
| `GET /admin/orgs/count` | Admin | Query: `search`, `createdAfter`, `createdBefore` | Count of organizations matching the filters. |
| `GET /admin/orgs/{orgID}` | Admin | Path: `orgID` | One organization, regardless of the caller's membership. Publishes `admin.org.viewed`. |
| `GET /admin/orgs/{orgID}/members` | Admin | Path: `orgID`; same `role`/`search`/`orderBy`/`orderDirection`/`offset`/`limit` params as `GET /auth/orgs/{orgID}/members` | That org's members, regardless of the caller's membership. Publishes `admin.org.viewed`. No total in the response — see `/count`. |
| `GET /admin/orgs/{orgID}/members/count` | Admin | Path: `orgID`; query: `role`, `search` | Count of that org's members matching the filters. No audit event (count only). |
| `GET /admin/users/{id}/orgs` | Admin | Path: `id`; query: `search` (name/slug), `role` (`owner`\|`admin`\|`member`), `orderBy` (`name` default, `created_at`, `member_count`), `orderDirection` (`asc` default, `desc`), `offset`, `limit` (omit for default `20`; `0` = unlimited; else capped at `100`) | Every org that user belongs to — the inverse of the members listing. `404` if `{id}` doesn't exist. No audit event. No total in the response — see `/count`. |
| `GET /admin/users/{id}/orgs/count` | Admin | Path: `id`; query: `search`, `role` | Count of that user's orgs matching the filters. |
| `POST /admin/orgs/{orgID}/members` | Admin | Path: `orgID`; body: `` `{userId, role}` `` | Force-adds a member — the recovery path for an org whose only owner is gone and is otherwise unmanageable by anyone. Publishes `admin.org.member.added`. |
| `DELETE /admin/orgs/{orgID}` | Admin | Path: `orgID` | Force-deletes the org. The org's name/slug are snapshotted into the `admin.org.deleted` event's metadata, since the row won't survive the delete for anything to look it up against afterward. |
| `DELETE /admin/orgs/{orgID}/members/{userID}` | Admin | Path: `orgID`, `userID` | Force-removes a member. Still refuses to remove an org's last owner — that repository-level invariant applies to an admin override too. Publishes `admin.org.member.removed`. |
| `PATCH /admin/orgs/{orgID}/members/{userID}/role` | Admin | Path: `orgID`, `userID`; body: `` `{role}` `` | Force-changes a member's role, including granting/revoking Owner — unlike the self-service version, does not require the caller to already be an Owner. Publishes `admin.org.member.role_changed`. |

## Admin — invites (`EnableInvite` required)

| Method & Path | Auth | Params | Description |
|---|---|---|---|
| `POST /admin/invites` | Admin | Body: `` `{email}` `` | Creates an invite. The inviting admin is taken from the session, not the body. |
| `GET /admin/invites` | Admin | Query: `offset`, `limit` (default `20`, max `100`); `search`, `status` (optional); `orderBy` (`created_at` default, `expires_at`, `email`, `status`); `orderDirection` (`desc` default, `asc`) | Lists invites. No total in the response — see `/count`. `status=expired` also matches pending invites past `expires_at`, and those rows are returned with `status: "expired"` (see note below). |
| `GET /admin/invites/count` | Admin | Query: `search`, `status` | Count of invites matching the filters. |
| `DELETE /admin/invites/{id}` | Admin | Path: `id` | Revokes (soft-cancels) an invite. |
| `POST /admin/invites/{id}/resend` | Admin | Path: `id` | Resends the invite email. |
| `DELETE /admin/invites/{id}/hard` | Admin | Path: `id` | Permanently deletes the invite record. |
| `POST /admin/invites/bulk/send` | Admin | Body: `` `{emails}` `` (max 25) | Creates and emails one invite per address. Returns `{succeeded, failed}`; addresses that already have an account or a live invite come back in `failed`. |
| `POST /admin/invites/bulk/resend` | Admin | Body: `` `{inviteIds}` `` (max 25) | Rotates each code and resends. Returns `{succeeded, failed}`. |
| `POST /admin/invites/bulk/revoke` | Admin | Body: `` `{inviteIds}` `` (max 100) | Revokes each invite. Returns `{succeeded, failed}`. |
| `POST /admin/invites/bulk/delete` | Admin | Body: `` `{inviteIds}` `` (max 100) | Permanently deletes each invite. Returns `{succeeded, failed}`. |

### Invite status and expiry

`expired` is derived, not stored. The terminal status is only written when
someone opens an invite link, so an invite that simply lapsed still sits in the
table as `pending`. `GET /admin/invites` closes that gap in both directions:

- `status=pending` matches only invites that are still valid.
- `status=expired` matches invites already written as expired **and** pending
  invites past `expires_at`.
- Rows are returned with the derived value, so a lapsed invite reads
  `status: "expired"` even though the stored column says `pending`.

The redemption endpoints are unaffected — they distinguish revoked from expired
themselves and write the terminal status.

### Bulk invite responses

Every bulk invite endpoint returns `{succeeded: string[], failed: [...]}` and is
**not** transactional, so both lists must be read. Failures carry `inviteId` for
the ID-keyed actions and `email` for `bulk/send`, where no invite exists yet.

`bulk/send` and `bulk/resend` cap at 25 per request rather than 100: each item
is an SMTP round-trip, dispatched across a small worker pool. Chunk larger
lists client-side.
